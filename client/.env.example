# ── TestMart client — environment (optional) ─────────────────────────────────
# Dev-only: override the /api proxy target if your API isn't on localhost:5000.
# VITE_API_TARGET=http://localhost:5000
